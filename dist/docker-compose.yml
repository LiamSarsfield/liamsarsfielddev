version: "3.7"
services:
  liamsarsfielddev:
    image: nginx:alpine
    container_name: webserver
    ports:
      - "8080:80"
    volumes:
      - ./spa:/var/www/public
      - ./build/nginx/conf.d/:/etc/nginx/conf.d/
